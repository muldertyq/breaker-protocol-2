# 构件可视化编辑器使用说明

本文说明《断路协议》构件编辑器当前支持的工作流、字段含义和常见数据规则。编辑器用于修改已经存在的构件 JSON；界面上的修改先进入内存，只有点击“保存构件”或按 `Ctrl+S` 后才写入文件。

## 1. 界面区域

### 顶部工具栏

- **保存构件**：校验并写入当前构件。保存成功或失败会显示 Toast。
- **视口居中**：重置中央画布的平移和缩放。
- **使用说明**：打开内置快速手册。
- **Gizmo 模式**：选择当前要在画布上编辑的空间数据。

### 左侧资产库

- **种族筛选**：显示全部种族或指定种族的构件。
- **分类筛选**：按 `Structural`、`Power`、`Propulsion`、`Weapons`、`Armor`、`Pipeline`、`Decorators` 筛选。
- **搜索**：按构件 ID 或显示名称搜索。
- **构件列表**：切换当前编辑对象。保存和热重载后会保持当前构件。

### 中央画布

- 鼠标滚轮：缩放。
- 鼠标中键拖动：平移。
- 右键在没有命中可删除对象时拖动：平移。
- 左键：在当前 Gizmo 模式下选择、创建或拖动对象。
- 右键：删除当前 Gizmo 模式下命中的对象。
- 底部状态栏显示鼠标所在的构件局部像素坐标。

### 右侧属性区

最上方是所有构件共有的基础数据。下方根据构件分类显示专属属性、贴图、效果、点位和引脚配置。

## 2. 推荐编辑流程

1. 在左侧筛选并选择构件。
2. 确认构件 ID、显示名称、种族、分类、尺寸、质量、耐久和装甲抗性。
3. 设置 Base、Overlay、Emissive 贴图。路径相对于 `core_data/`，不要填写 `res://core_data/` 前缀。
4. 修改该分类的专属参数。
5. 在顶部切换 Gizmo，编辑引脚、发射口、弹位、跑道或其他空间点位。
6. 对武器或机库开启测试，检查瞄准、开火、导弹舱盖或无人机弹射。
7. 按 `Ctrl+S` 保存；看到绿色 Toast 才表示文件已经写入。

## 3. 基础字段

- **构件 ID**：数据引用使用的唯一标识。不要随意修改现有 ID，否则其他配置中的引用可能失效。
- **显示名称**：编辑器和游戏界面展示的名称。
- **所属种族**：决定构件归属和左侧筛选结果；`Universal` 表示通用构件。
- **构件分类**：决定右侧出现哪组专属编辑器，也决定运行时如何解析 `properties`。
- **宽度/高度**：单位为 GU，当前画布中 `1 GU = 80 px`。
- **结构耐久**：构件本体生命值，必须大于 0。
- **Base 贴图**：固定底层。
- **Overlay 贴图**：炮身、顶盖等可独立旋转或覆盖的图层。
- **Emissive 贴图**：发光图层，可选择跟随 Overlay 或固定在 Base。

## 4. 构件分类

| 分类 | 用途 | 主要专属内容 |
| --- | --- | --- |
| `Structural` | 船体结构 | 基础几何、质量、耐久、装甲 |
| `Power` | 能源构件 | 发电、储能等能源参数 |
| `Propulsion` | 推进构件 | 推力、能耗、喷口点位 |
| `Weapons` | 武器和机库 | 载荷、战斗参数、目标筛选、发射点或机库跑道 |
| `Armor` | 装甲或护盾 | 装甲偏折；带 Shield 标签时显示护盾参数 |
| `Pipeline` | 管线和芯片 | 管线传输与拓扑参数 |
| `Decorators` | 装饰增强模块 | 适用条件、效果和属性修改器 |

机库仍属于 `Weapons` 分类，但 `mountType` 使用 `Hangar`。编辑器会显示无人机和跑道配置，而不是常规武器配置。

## 5. 引脚与端口

引脚描述构件如何接入舰船的资源和控制网络。装饰模块、武器、能源构件等需要参与网络连接的组件都应配置正确的引脚。

### 创建和移动

1. 顶部 Gizmo 选择 **Pins**。
2. 在构件网格边缘左键创建引脚。
3. 左键拖动已有引脚调整位置。
4. 在画布上右键引脚，或在右侧点击删除按钮，删除引脚。

### 字段含义

- **IN**：构件接收资源或信号。
- **OUT**：构件输出资源或信号。
- **Universal**：通用自适应连接。
- **PulsePower**：常规脉冲电力。
- **HeavyPulse**：重型高压电力。
- **Thermal**：热量传输或散热。
- **Logic**：控制和逻辑信号。

引脚 ID 根据类别、方向和位置自动生成。引脚必须位于构件尺寸范围内，并附着在 `Top`、`Bottom`、`Left`、`Right` 四种边缘之一。同一构件中引脚 ID 不能重复。

## 6. 武器配置

### 挂载方式

- **Fixed**：固定武器，不进行炮塔回转。
- **Turret**：可回转炮塔。需要设置底座安装位、贴图旋转中心、射界和回转速度。
- **Hangar**：无人机机库，使用机库专属配置。

### 载荷类型

- **Ballistic**：普通实体弹丸。
- **PulseBeam**：有持续时长的脉冲激光。
- **ContinuousBeam**：按住开火时持续显示的光束。
- **Missile**：使用弹位、舱盖、制导和装填逻辑的导弹或鱼雷。

### 目标筛选

武器和无人机使用相同的三层目标筛选：

- **可命中类型**：目标命中任意一个选中类型即可。空选表示不限制类型。
- **必须标签**：目标必须同时拥有全部选中标签。空选表示不限制标签。
- **排除标签**：目标只要拥有任意一个选中标签就会被拒绝。

例如：可命中类型选择 `Drone` 和 `Missile`，必须标签选择 `Mechanical`，排除标签选择 `Friendly`，表示只能攻击机械类无人机或导弹，并且不攻击友军。

同一个标签不能同时放进“必须标签”和“排除标签”，否则保存校验失败。

### 导弹、鱼雷和无人机生命值

- 导弹和鱼雷使用武器属性中的 **弹体生命值**。
- 无人机使用机库属性中的 **单机生命值**。
- 两者都必须大于 0，供拦截和摧毁逻辑使用。

## 7. 多开火点与时序

常规弹道武器、脉冲激光和持续光束都支持多个开火点。

### 编辑方法

1. 顶部 Gizmo 选择 **Muzzles**。
2. 在画布上左键添加或选择开火点，也可在右侧列表使用 `+` 添加。
3. 拖动画布标记，或直接修改右侧局部 X/Y。
4. 设置唯一的开火点 ID、方向偏移和 `sequenceIndex`。

### 时序规则

- 相同 `sequenceIndex`：一次射击中同时齐射。
- 不同 `sequenceIndex`：按组号排序，每次射击在各组之间轮流开火。

| 发射口 | sequenceIndex | 行为 |
| --- | ---: | --- |
| 左炮口 | 0 | 与右炮口同时开火 |
| 右炮口 | 0 | 与左炮口同时开火 |
| 中央炮口 | 1 | 下一次射击单独开火 |

`angleOffset` 是相对于武器当前瞄准方向的角度偏移，可用于固定散射炮口。

### 开火测试

在武器属性中开启“实时瞄准与开火测试”，移动鼠标控制瞄准，按住左键开火。切换到其他构件时测试模式会自动关闭，非武器不会响应开火输入。

## 8. 导弹弹位、舱盖和无人机跑道

### 导弹舱盖 Bays

舱盖定义开合区域、开盖耗时和动画方式。内置导弹在发射前会先检查对应舱盖是否已经打开。

### 弹位 Munition Slots

弹位定义弹体在构件上的位置、角度和所属舱盖。发射模式决定逐发、连发或齐射，装填模式决定整架装填或单发独立装填。

### 无人机跑道 Runways

每条跑道包含起始点、离舰点、弹射时长、离舰初速和发射顺序。起始点是无人机的初始位置；离舰点决定弹射结束位置和离舰方向。

## 9. 装饰模块

装饰模块分为三层：**适用条件 → 效果 → 属性修改器**。

### 适用条件

适用条件决定这个装饰模块能作用到什么对象，不直接定义数值变化。

- **武器标签**：武器必须满足所选标签。
- **载荷类型**：命中任意所选类型即可，例如 `Ballistic`、`PulseBeam`、`ContinuousBeam`、`Missile`。
- **目标标签**：命中目标必须具备这些标签，主要用于 `OnHit` 效果，例如只对 `Mechanical` 生效。
- 所有条件都不选择时表示不限制。

这里的“目标标签”不是武器自动选敌规则。武器打谁由武器或无人机的“目标筛选”决定；装饰模块的目标标签只决定效果在命中后是否满足应用条件。

### 效果列表

效果是属性修改器的父级，负责定义效果 ID、名称、触发方式和叠加规则：

- `Passive`：安装并满足条件时常驻。
- `OnFire`：武器开火时触发。
- `OnHit`：命中目标时触发。
- `Interval`：按周期触发。
- `Additive`：多个来源的修改值累加。
- `Highest`：多个来源中只取最高值。
- `Independent`：每个效果实例独立计时和生效。

### 属性修改器

属性修改器定义“具体改哪个属性、怎么改、改多少”。一个效果可以添加多个修改器，只要它们共享相同的触发方式和叠加规则。

- **目标属性**：例如 `Damage`、`FireRate`、`MoveSpeed`、`StatusDuration`。
- **运算方式**：`Increase` 增加、`Decrease` 减少、`Set` 直接设定。
- **数值类型**：`Percent` 百分比、`Flat` 固定数值。
- **数值**：填写正数。正负方向由运算方式决定，不要填写负数。

#### 示例 1：常驻伤害 +10%

- 效果：`Passive` + `Additive`。
- 修改器：`Damage` + `Increase` + `Percent` + `10`。

#### 示例 2：命中后减速 20%，持续 1 秒

- 效果：名称“冷冻命中”，`OnHit` + `Highest`。
- 修改器 1：`MoveSpeed` + `Decrease` + `Percent` + `20`。
- 修改器 2：`StatusDuration` + `Set` + `Flat` + `1`。

这两个修改器属于同一个效果，因为它们在同一次命中中一起触发，并共享同一个叠加策略。

#### 示例 3：每 5 秒过载射击，伤害 +40%

- 效果：`Interval` + `Independent`。
- 修改器 1：`TriggerInterval` + `Set` + `Flat` + `5`。
- 修改器 2：`Damage` + `Increase` + `Percent` + `40`。

不同触发方式或不同叠加策略的能力应拆成多个效果。

## 10. 保存校验与排错

保存时会运行 `DataValidator`。错误会阻止保存，警告会写入 Godot 控制台但不一定阻止保存。

常见错误：

- 构件 ID 或种族为空。
- 尺寸小于 1，结构耐久不大于 0。
- 引脚 ID 重复、位置越界或边缘非法。
- 开火点 ID 为空或重复，`sequenceIndex` 小于 0。
- 导弹弹体生命值或无人机生命值不大于 0。
- 必须目标标签与排除目标标签冲突。
- 装饰模块没有效果，效果没有修改器，效果 ID 重复。
- 装饰修改器数值不大于 0，或百分比爆炸概率超过 100。

保存失败时先查看红色 Toast，再查看 Godot 控制台中以 `DataValidator` 开头的具体消息。

## 11. 数据边界

编辑器负责编辑、序列化、校验和可视化预览数据。装饰效果、目标筛选、生命值等字段最终如何参与正式战斗，由运行时战斗系统读取并执行。画布中的开火、导弹和无人机属于编辑预览，不等同于完整战斗模拟。
